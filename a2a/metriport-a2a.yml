generated: '2026-08-14'
method: probed
source: https://docs.metriport.com/.well-known/agent-card.json
description: >-
  Metriport serves an A2A Agent Card from its documentation host. The card
  describes a documentation-assistant agent rather than the Medical API itself:
  its single skill points at a published Agent Skill markdown file, and its only
  interface is the docs site. The companion MCP server at
  https://docs.metriport.com/mcp (see mcp/metriport-mcp.yml) is the executable
  half of the same surface. Saved verbatim to metriport-agent-card.json — this
  artifact is the manifest and the grade.
card: metriport-agent-card.json
discovery:
  path: /.well-known/agent-card.json
  canonical: true
  host: docs.metriport.com
x-evidence:
  fetched: '2026-08-14'
  url: https://docs.metriport.com/.well-known/agent-card.json
  http_status: 200
  content_type: application/json
identity:
  name: Metriport
  url: https://docs.metriport.com/
  version: '1.0.0'
  protocolVersion: '0.3'
  preferredTransport: HTTP+JSON
  provider:
    organization: Metriport
    url: https://docs.metriport.com/
  documentationUrl: https://docs.metriport.com/
capabilities:
  streaming: false
  pushNotifications: false
skills:
  - id: metriport
    name: Metriport
    url: https://docs.metriport.com/.well-known/agent-skills/metriport/skill.md
    saved_as: ../skills/metriport-medical-api-skill.md
    summary: >-
      Building healthcare applications that access, manage and exchange patient
      medical data across HIEs, pharmacies and laboratories — patient records,
      consolidated FHIR queries, secure provider messaging, facility management,
      and contributing data back to health networks.
grade: conformant
grade_basis: >-
  Graded against A2A 1.0.0. All three hard checks pass: capabilities is a JSON
  object, protocolVersion is present, and skills is an array. The optional
  fields the grade also looks for — preferredTransport, defaultInputModes,
  defaultOutputModes — are all present.
deviations:
  - field: protocolVersion
    observed: '0.3'
    note: >-
      The card declares A2A 0.3, not 1.0.0. It is a pre-1.0 card that happens to
      satisfy the 1.0 hard checks; it is not a card authored against 1.0.
  - field: description
    observed: absent
    note: >-
      A2A lists description among the required AgentCard fields. This card omits
      it at the top level; the human-readable description exists only inside
      skills[0].description.
  - field: supportedInterfaces
    observed: present
    note: >-
      A2A 1.0.0 names the additional-transport array additionalInterfaces.
      This card uses the 0.3-era supportedInterfaces key, and its single entry
      points at the docs root rather than at an A2A JSON-RPC endpoint.
  - field: url
    observed: https://docs.metriport.com/
    note: >-
      The service URL is the documentation site root, not an agent endpoint that
      accepts A2A messages. No A2A transport was reachable there; the callable
      surface for this agent is the MCP endpoint at /mcp, which the card does
      not name.
  - field: scope
    observed: documentation agent
    note: >-
      The card is generated by the Mintlify docs platform and covers the
      documentation corpus. It does not advertise the Medical API's own
      operations, and it should not be read as an agent surface over patient
      data.
maintainers:
  - FN: Kin Lane
    email: kin@apievangelist.com
